var name = @COMPUTERNAME
var example = 25

another {
    echo test
}

prerun {
    echo world
}

run (arg0, opt arg1) < prerun, another {
    var cool = &example
    var test = &cool
    $ echo &prerun.0
    $ echo &another.0
    $ python run.py arg0 arg1 &name &test
}

_ {
    $ echo import sys > run.py
    $ echo print(sys.argv[1:]) >> run.py
}