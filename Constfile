var name = @COMPUTERNAME
var example = 25

prerun {
    echo hello
}

run (arg0, opt arg1) < prerun {
    var cool = &example
    var test = &cool
    $ python run.py arg0 arg1 &name &prerun.0
}

_ {
    $ echo import sys > run.py
    $ echo print(sys.argv[1:]) >> run.py
}