*.py
*.env
diagram.md
debug.json
.zcode

# Compiled binary (the README suggests `go build -o construct`)
/construct
/construct.exe

# File dependency cache
.construct-cache/

# LSP dev mode marker (points to source server binary)
.construct-lsp-dev

# LSP server build outputs (binaries, go-tool ~ renames, packaging stash)
editor/vscode/server/construct-lsp*
editor/vscode/*.vsix
