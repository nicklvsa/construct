var app = myapp

# Installs dependencies
setup {
    $ python -m pip install -r requirements.txt
}

# Runs the test suite
test < setup {
    $ python -m pytest
}

# Lints the code
lint {
    $ python -m ruff check .
}

_ < test, lint {
    $ echo "all checks passed"
}
